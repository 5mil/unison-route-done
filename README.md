# unison-route-done
A small Unison library providing typed early-exit for route handlers via a Route.Done ability — commit a response and stop handler execution without threading Abort through your codebase.
